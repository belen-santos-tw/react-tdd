import React from 'react';
import {Square} from "./Square";

const Board = () => (
    <div>
        <Square position={1} onClick={() => {}} />
        <Square position={2} onClick={() => {}} />
        <Square position={3} onClick={() => {}} />
        <Square position={4} onClick={() => {}} />
        <Square position={5} onClick={() => {}} />
        <Square position={6} onClick={() => {}} />
        <Square position={7} onClick={() => {}} />
        <Square position={8} onClick={() => {}} />
        <Square position={9} onClick={() => {}} />
    </div>
)

export default Board;